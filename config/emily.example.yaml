# Reserved for future tool and provider configuration. Environment variables are
# the supported configuration source in Emily Core v0.1.
assistant:
  name: Emily
providers:
  local:
    enabled: true
  # ollama:
  #   enabled: false
  #   base_url: http://host.docker.internal:11434

